<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context"
	xmlns:aop="http://www.springframework.org/schema/aop" xmlns:tx="http://www.springframework.org/schema/tx"
	xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
            http://www.springframework.org/schema/aop 
            http://www.springframework.org/schema/aop/spring-aop-3.0.xsd
            http://www.springframework.org/schema/tx 
            http://www.springframework.org/schema/tx/spring-tx-3.0.xsd
            http://www.springframework.org/schema/context 
            http://www.springframework.org/schema/context/spring-context-3.0.xsd"
	default-autowire="byName">

	<context:component-scan base-package="com.hbird.common.core.redis" />
	
	<bean id="redisQueryAdvice" class="com.hbird.common.core.redis.RedisQueryAdvice">
		<property name="cache" value="${redis.isCache}" />
		<property name="annotationCache" value="${redis.isAnnotationCache}" />
	</bean>
	<bean id="redisChangeAdvice" class="com.hbird.common.core.redis.RedisChangeAdvice">
		<property name="cache" value="${redis.isCache}" />
		<property name="annotationCache" value="${redis.isAnnotationCache}" />
	</bean>

	<aop:config>
		<!-- 查询方法切面 -->
		<!-- 查询 -->
		<aop:advisor id="redisQueryAdviceAop" advice-ref="redisQueryAdvice"
			pointcut="execution(* com.hbird.portal.manager.impl..*.query*(..)) " />
		<!-- 根据ID获取实体 -->
		<aop:advisor id="redisQueryGetAdviceAop" advice-ref="redisQueryAdvice"
			pointcut="execution(* com.hbird.portal.manager.impl..*.get*(..)) " />

		<!-- 操作方法切面 -->
		<!-- 插入 -->
		<aop:advisor id="redisChangeInsertAdviceAop" advice-ref="redisChangeAdvice"
			pointcut="execution(* com.hbird.portal.manager.impl..*.insert*(..)) " />
		<!-- 更新 -->
		<aop:advisor id="redisChangeUpdatetAdviceAop" advice-ref="redisChangeAdvice"
			pointcut="execution(* com.hbird.portal.manager.impl..*.update*(..)) " />
		<!-- 删除 -->
		<aop:advisor id="redisChangeDelAdviceAop" advice-ref="redisChangeAdvice"
			pointcut="execution(* com.hbird.portal.manager.impl..*.del*(..)) " />
	</aop:config>

	<bean id="jedisPoolConfig" class="redis.clients.jedis.JedisPoolConfig">
		<property name="maxActive" value="${redis.maxActive}" />
		<property name="maxIdle" value="${redis.maxIdle}" />
		<property name="maxWait" value="${redis.maxWait}" />
		<property name="testOnBorrow" value="${redis.testOnBorrow}" />
	</bean>

	<bean id="jedisPool" class="redis.clients.jedis.JedisPool">
		<constructor-arg index="0" ref="jedisPoolConfig" />
		<constructor-arg index="1" value="${redis.host}" />
		<constructor-arg index="2" value="${redis.port}" type="int" />
		<constructor-arg index="3" value="${redis.timeout}"
			type="int" />
		<constructor-arg index="4" value="${redis.password}" />
	</bean>

</beans>